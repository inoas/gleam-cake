---
version: 2.0.0
title: insert_on_conflict_ignore_where_ordering_test
file: ./test/cake_test/insert_on_conflict_ignore_test.gleam
test_name: insert_on_conflict_ignore_where_ordering_test
---
#(
  PreparedStatement(
    "$",
    "INSERT INTO counters (name, counter) VALUES ($1, $2) ON CONFLICT (name) WHERE counters.is_active IS TRUE DO NOTHING",
    [
      StringParam("Whiskers"),
      IntParam(1),
    ],
    2,
    Postgres,
  ),
  PreparedStatement(
    "?",
    "INSERT INTO counters (name, counter) VALUES (?1, ?2) ON CONFLICT (name) WHERE counters.is_active IS TRUE DO NOTHING",
    [
      StringParam("Whiskers"),
      IntParam(1),
    ],
    2,
    Sqlite,
  ),
  PreparedStatement(
    "?",
    "INSERT INTO counters (name, counter) VALUES (?, ?) ON CONFLICT (name) WHERE counters.is_active IS TRUE DO NOTHING",
    [
      StringParam("Whiskers"),
      IntParam(1),
    ],
    2,
    Maria,
  ),
  PreparedStatement(
    "?",
    "INSERT INTO counters (name, counter) VALUES (?, ?) ON CONFLICT (name) WHERE counters.is_active IS TRUE DO NOTHING",
    [
      StringParam("Whiskers"),
      IntParam(1),
    ],
    2,
    Mysql,
  ),
)
