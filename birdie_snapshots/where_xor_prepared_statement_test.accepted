---
version: 1.1.6
title: where_xor_prepared_statement_test
file: ./test/cake/internal/query/where_test.gleam
test_name: where_xor_prepared_statement_test
---
#(
  PreparedStatement(
    "$",
    "SELECT * FROM cats WHERE ((name = $1) AND NOT (is_wild IS TRUE) AND NOT (age <= $2)) OR (NOT (name = $3) AND (is_wild IS TRUE) AND NOT (age <= $4)) OR (NOT (name = $5) AND NOT (is_wild IS TRUE) AND (age <= $6)) OR (NOT (name = $7) AND NOT (is_wild IS TRUE) AND NOT (age <= $8))",
    [
      StringParam("Karl"),
      IntParam(9),
      StringParam("Karl"),
      IntParam(9),
      StringParam("Karl"),
      IntParam(9),
      StringParam("Karl"),
      IntParam(9),
    ],
    8,
    PostgresAdapter,
  ),
  PreparedStatement(
    "?",
    "SELECT * FROM cats WHERE ((name = ?1) AND NOT (is_wild IS TRUE) AND NOT (age <= ?2)) OR (NOT (name = ?3) AND (is_wild IS TRUE) AND NOT (age <= ?4)) OR (NOT (name = ?5) AND NOT (is_wild IS TRUE) AND (age <= ?6)) OR (NOT (name = ?7) AND NOT (is_wild IS TRUE) AND NOT (age <= ?8))",
    [
      StringParam("Karl"),
      IntParam(9),
      StringParam("Karl"),
      IntParam(9),
      StringParam("Karl"),
      IntParam(9),
      StringParam("Karl"),
      IntParam(9),
    ],
    8,
    SqliteAdapter,
  ),
)